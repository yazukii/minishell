/*
#include "../../headers/minishell.h"

int pwd(t_list_tokken tokken, t_parsing *bag)
{
    if (tokken.args)
    {
        printf("Too many arguments\n");
        return (1);
    }
    printf("%s\n", bag->cwd);
    return (0);
}*/
