#include "minishell.h"

/*
int printenv(char **envi)
{
    int i;

    i = 0;
    while (envi[i])
    {
        printf("%s\n", envi[i]);
        i++;
    }
    return (0);
}

int env(t_list_tokken tokken, t_parsing bag)
{
	return (printenv(bag.env));
}*/
